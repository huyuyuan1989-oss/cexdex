name: Chain Money Flow Monitor

on:
  schedule:
    # 每 15 分鐘執行一次 (大幅提升 Paper Trading 止損反應速度)
    - cron: '*/15 * * * *'
  # 允許手動觸發
  workflow_dispatch:

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要寫入權限來提交資料庫和報告

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Main Pipeline
        env:
          # Discord Webhook URL (設定在 GitHub Secrets 中)
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          echo "🚀 Pipeline started"
          python main.py
          echo "✅ Pipeline finished"

      - name: Commit and Push Data & Reports
        run: |
          git config --global user.name 'GitHub Action Bot'
          git config --global user.email 'action@github.com'
          git add reports/*.json reports/*.csv reports/*.html reports/*.md
          git add *.db *.json || true
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📈 Auto-update: Dashboards and Persistent DB [skip ci]"
            git push
          fi
